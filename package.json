{
  "name": "atlas-ai",
  "private": true,
  "scripts": {
    "build": "yarn workspace atlas-wire build && yarn workspace atlas-agent build && yarn workspace atlas-app-logs build && yarn workspace atlas-gateway build && yarn workspace atlas-cli build",
    "test": "yarn workspaces run test",
    "dev": "yarn workspace atlas-gateway dev",
    "start": "yarn workspace atlas-cli start"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@1.22.22"
}
